{
  "name": "paylite",
  "version": "1.0.0",
  "description": "PayLite — Payroll management for Indian micro-businesses",
  "private": true,
  "scripts": {
    "install:all": "cd backend && npm install && cd ../frontend && npm install",
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "cd backend && npm run dev",
    "dev:frontend": "cd frontend && npx ng serve --proxy-config proxy.conf.json --open",
    "test": "cd backend && npx vitest run",
    "db:push": "cd backend && npx prisma db push",
    "db:seed": "cd backend && npx prisma db seed",
    "db:generate": "cd backend && npx prisma generate",
    "start": "cd backend && npx tsx src/server.ts"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
